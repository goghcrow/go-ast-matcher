
-- adder.stdout --
node: atomic.AddInt32(&x, 1)

node: atomic.AddUintptr(&ptr, 1)

-- a.go --
package a

import (
	"sync/atomic"
)

func main() {
    var x int32
	atomic.AddInt32(&x, 1)

	var ptr uintptr
	atomic.AddUintptr(&ptr, 1)
}